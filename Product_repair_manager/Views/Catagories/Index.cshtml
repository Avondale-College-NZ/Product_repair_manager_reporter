@model IEnumerable<Product_repair_manager.Models.Catagory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Catagory_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Items)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.Catagory_Name)
            </td>
            <td>
                @Html.DisplayFor(model => item.Items)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CatagoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CatagoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CatagoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
